#include <stdio.h>
#include "no_duplo.h"

int main () {
	t_apont_no_duplo ap_no;
	t_apont_no_duplo v_no[1000000];
	int i = 1;
	while (ap_no = cria_no_duplo (i++));
	printf ("\n%d\n", i);
	return 0;
}